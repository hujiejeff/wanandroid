apply from: "../../module.build.gradle"

android {
    defaultConfig {
        if (isBuildModule.toBoolean()) {
            applicationId 'com.hujiejeff.wanandroid.login'
        }
    }

    resourcePrefix "login_"
}

dependencies {
    api project(":module:base")
}